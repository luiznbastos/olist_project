# Copy to .env and fill in values before running docker compose up

# Required
OPENAI_API_KEY=sk-...

# Optional — defaults shown
OPENAI_MODEL=gpt-4o-mini
CUBEJS_API_SECRET=dev-secret
DUCKDB_LAYERS=Silver
